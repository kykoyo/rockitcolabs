<h2>Day Pass</h2>

<h3>1 day pass</h3>
<%= form_tag charges_path do %>

  <%= hidden_field_tag :day, 1 %>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="1 Day Pass"
          data-amount="1500"></script>
<% end %>

<h3>3 day passes</h3>
<%= form_tag charges_path do %>

  <%= hidden_field_tag :day, 3 %>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="3 Day Pass"
          data-amount="2500"></script>
<% end %>

<h3>5 day passes</h3>
<%= form_tag charges_path do %>

  <%= hidden_field_tag :day, 5 %>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="5 Day Pass"
          data-amount="4000"></script>
<% end %>

